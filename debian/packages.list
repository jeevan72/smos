# Core System
ubuntu-desktop
ubuntu-drivers-common
# The interceptor hooks into bash's command_not_found_handle.
# Install the default handler so unknown commands still give a sensible error.
command-not-found

# Web
firefox

# Office
libreoffice
libreoffice-calc
libreoffice-writer
libreoffice-impress

# Media
vlc
gimp
shotwell
cheese

# Utilities
gnome-tweaks
gnome-shell-extensions
dconf-editor
synaptic
gdebi
timeshift
stacer
gnome-terminal

# System Tools
htop
neofetch
curl
wget
git
nano
vim

# Python (for SimpleMode assistant)
python3
python3-pip
python3-venv

# Linutil (System Toolbox) is NOT installed via apt — it is distributed as a
# single release binary. chroot-setup.sh downloads the pinned release into
# /usr/local/bin/linutil and creates the desktop launcher.

# Network
network-manager
network-manager-gnome

# Accessibility
gnome-accessibility-themes
orca
onboard

# Fonts
fonts-noto
fonts-noto-mono
fonts-liberation
fonts-ubuntu

# Printing
cups
system-config-printer
hplip
printer-driver-all
